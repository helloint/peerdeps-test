{
  "name": "peerdeps-test",
  "version": "1.0.0",
  "private": true,
  "description": "Test 3rd Party Tools to install direct `peerDependencies`",
  "scripts": {
    "prepare": "npm-install-peers",
    "-1prepare": "install-peers",
    "-2prepare": "install-self-peers --npm -- --ignore-scripts"
  },
  "author": "Wayne",
  "license": "ISC",
  "devDependencies": {
    "@team-griffin/install-self-peers": "^1.1.1",
    "install-peers-cli": "^2.2.0",
    "npm-install-peers": "^1.2.2"
  },
  "peerDependencies": {
    "@babel/cli": ">=7"
  }
}
